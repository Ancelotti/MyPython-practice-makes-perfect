map 是关于列表的方法，其目的是将列表中的元素做某种处理，然后返回一个新列表
如map（function,[]）,function可以是定义好的函数，可以是匿名函数，可以是内置函数等等

而max方法，顾名思义是寻找最大值，返回最大值，但是其内置key参数，可以自定义比较大小的方式，
比如a=[-1,2,-3,1]最大值为2，但是如果加上key=lambda x:x**2,就定义了比较方法：是以x的平方作为比较方式，
其中-3的平方最大，所以在这种比较方式下，-3为最大，故返回-3。

by Ancelotti
